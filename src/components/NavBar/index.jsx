import './styles.css'

import { Button } from 'antd'

export default function Login() {
    return (
        <nav>
            <div className="navLeft">
                <h1>TalkTo</h1>
            </div>

            <div className="navRight">
                <h4>Disconnected</h4>
            </div>
        </nav>
    )
}